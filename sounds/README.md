Place deployed audio assets in this folder.

Preferred fire audio filename:
- `fireplace-with-crackling-sounds-2-min-rk-178392.mp3`

Fallback fire audio filename:
- `campfire_sound_ambience.ogg`

Deployment notes:
- Keep these files on the same origin as `index.html`.
- Serve over HTTPS in production.
- Browsers block autoplay with sound, so users must press the existing sound button once.
